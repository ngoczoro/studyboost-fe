FROM node:20-alpine
WORKDIR /app

# Cài dependencies trước (cache layer)
COPY package*.json ./
RUN npm ci

# Source code được mount qua volume — không COPY ở đây
# → thay đổi file trên máy sẽ reflect ngay trong container

EXPOSE 3000

ENV NEXT_TELEMETRY_DISABLED=1

CMD ["npm", "run", "dev"]
